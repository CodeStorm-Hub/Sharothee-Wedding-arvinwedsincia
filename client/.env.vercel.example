# Vercel Production Environment Variables
# Add these to your Vercel Project Settings → Environment Variables

# Site Configuration
NEXT_PUBLIC_SITE_URL="https://your-project.vercel.app"
NODE_ENV="production"

# Database (Recommended: Use Vercel Postgres or External PostgreSQL)
# Option 1: Vercel Postgres (Recommended)
DATABASE_PROVIDER="postgresql"
POSTGRES_URL="postgres://username:password@hostname:5432/database?sslmode=require"
POSTGRES_PRISMA_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY"
DATABASE_URL="${POSTGRES_PRISMA_URL}"

# Option 2: External PostgreSQL (Supabase, Railway, PlanetScale)
# DATABASE_PROVIDER="postgresql"
# DATABASE_URL="postgresql://user:pass@host:5432/db?sslmode=require"

# Option 3: MySQL
# DATABASE_PROVIDER="mysql"
# DATABASE_URL="mysql://user:pass@host:3306/database"

# NextAuth Configuration
NEXTAUTH_SECRET="GENERATE_STRONG_SECRET_32_CHARS_MINIMUM"
NEXTAUTH_URL="https://your-project.vercel.app"

# Admin Credentials
ADMIN_EMAIL="admin@yourdomain.com"
ADMIN_PASSWORD="STRONG_SECURE_PASSWORD"

# Email Service (Gmail SMTP)
GMAIL_USER="your-email@gmail.com"
GMAIL_APP_PASSWORD="your-gmail-app-password"
GMAIL_FROM="Your Wedding <noreply@yourdomain.com>"
TEST_EMAIL_TO="admin@yourdomain.com"

# Cloudinary (for media uploads)
CLOUDINARY_CLOUD_NAME="your-cloudinary-cloud-name"
CLOUDINARY_API_KEY="your-cloudinary-api-key"
CLOUDINARY_API_SECRET="your-cloudinary-api-secret"

# Optional: Google Analytics
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# Optional: Google Site Verification
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION="your-verification-code"

# Optional: Custom Domain
# NEXT_PUBLIC_DOMAIN="yourdomain.com"

# ========================================
# IMPORTANT NOTES:
# ========================================
# 1. Generate NEXTAUTH_SECRET with: openssl rand -base64 32
# 2. Set all variables in Vercel Dashboard → Settings → Environment Variables
# 3. Apply to: Production, Preview, and Development environments
# 4. After adding variables, redeploy your application
# 5. Gmail App Password: https://support.google.com/accounts/answer/185833
# 6. For database migration: Run migrations before first deployment
# ========================================
